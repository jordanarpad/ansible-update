---
  - hosts: pi1b
    remote_user: jordana
    tasks:
      - name: update pihole
        command: "pihole -up"
  - hosts: pi
    remote_user: jordana
    tasks:
      - become: yes
        apt:
          force_apt_get: yes
          update_cache: yes
          name: "*"
          state: latest
